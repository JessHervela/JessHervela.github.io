name: CI

on:
 workflow_dispatch

jobs:
 github-pages:
  runs-on: ubuntu-latest
  environment: github-pages
  env:
   APIKey: ${{ secrets.TOKEN }}
  steps:
   - name: Read env
     run: echo "env ${{secrets.TOKEN}}"
